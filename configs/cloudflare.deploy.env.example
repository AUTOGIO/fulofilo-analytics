# Copy to repo root as `.env.cloudflare` (gitignored via `.env.*`).
# Workers deploy: Edit Cloudflare Workers + Zone (giovannini.us) as wrangler needs.
# Tunnel/DNS cleanup script: Account → Cloudflare Tunnel → Edit; Zone → DNS → Edit.
#   python3 scripts/cloudflare_dashboard_cleanup.py
CLOUDFLARE_API_TOKEN=
