# Optional overrides for bundled local automations (copy to .env or export in shell).
# Defaults work after clone without setting these.

# LOYVERSE_DATA_ROOT=/path/to/fulofilo-analytics/automations/loyverse-data
# REDE_AUTOMATION_ROOT=/path/to/fulofilo-analytics/automations/rede-automation
# REDE_DOWNLOAD_DIR=$HOME/Downloads/Rede
